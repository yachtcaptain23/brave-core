diff --git a/third_party/blink/renderer/core/frame/local_frame.h b/third_party/blink/renderer/core/frame/local_frame.h
index 92ea3730539e5d6a4627cd55e305eaa36ed41d3d..eb843afdeb933be06503b40a3acd8b741e8518a8 100644
--- a/third_party/blink/renderer/core/frame/local_frame.h
+++ b/third_party/blink/renderer/core/frame/local_frame.h
@@ -54,6 +54,11 @@
 #include "third_party/blink/renderer/platform/scheduler/public/frame_scheduler.h"
 #include "third_party/blink/renderer/platform/supplementable.h"
 
+#include "brave/third_party/blink/brave_page_graph/buildflags/buildflags.h"
+#if BUILDFLAG(BRAVE_PAGE_GRAPH_ENABLED)
+#include "brave/third_party/blink/brave_page_graph/types.h"
+#endif
+
 namespace base {
 class SingleThreadTaskRunner;
 }
@@ -433,6 +438,14 @@ class CORE_EXPORT LocalFrame final : public Frame,
   // To be called from OomInterventionImpl.
   void ForciblyPurgeV8Memory();
 
+#if BUILDFLAG(BRAVE_PAGE_GRAPH_ENABLED)
+  void SetPageGraphChildFrameId(const ::brave_page_graph::ChildFrameId child_frame_id);
+  ::brave_page_graph::ChildFrameId GetPageGraphChildFrameId() const;
+
+  void SetPageGraphNodeIdInParentDocument(const DOMNodeId node_id);
+  DOMNodeId GetPageGraphNodeIdInParentDocument() const;
+#endif
+
  private:
   friend class FrameNavigationDisabler;
 
@@ -563,6 +576,11 @@ class CORE_EXPORT LocalFrame final : public Frame,
   const bool is_save_data_enabled_;
 
   std::unique_ptr<FrameOverlay> frame_color_overlay_;
+
+#if BUILDFLAG(BRAVE_PAGE_GRAPH_ENABLED)
+  ::brave_page_graph::ChildFrameId page_graph_child_frame_id_ = 0;
+  DOMNodeId dom_node_id_in_parent_document_ = 0;
+#endif
 };
 
 inline FrameLoader& LocalFrame::Loader() const {
