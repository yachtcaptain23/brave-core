GraphItem
  - id
  Edge
    - inNode
    - outNode
    Attribute
      - name
      Delete  ({NodeParser, NodeScript} -> NodeHTML)
      Set     ({NodeParser, NodeScript} -> NodeHTML)
        - value
    Node
      Create  ({NodeParser, NodeScript} -> NodeHTML)
      Insert  ({NodeParser, NodeScript} -> NodeHTML)
        - parentNodeId
        - beforeNodeId
      Remove  (NodeScript -> NodeHTML)
    Call      (NodeScript -> {NodeWebAPI, NodeShields})
      - methodName
      - arguments
    Request   ({NodeParser, NodeScript} -> {Node_Resource, NodeShields})
      - url
      - type
    Storage   ({NodeScript, NodeResource} -> NodeStorage)
      - key
      Set
        - value
      Delete
      Read

  Node
    - parentNodes
    - childNodes
    Parser (singleton)
    Storage (abstract)
      CookieJar (singleton)
      LocalStorage (singleton)
    WebAPI (singleton)
    Shields (singleton)
    HTML (fetchable by DOMNodeId)
      - DOMNodeId
      - tagName
    Script (fetchable by ScriptId)
      - scriptId
      WASM
      Classic
    Resource