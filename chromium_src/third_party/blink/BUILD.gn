source_set("blink") {
  configs += [
    "//third_party/blink/renderer:config",
  ]

  sources = [
    "render/modules/bluetooth/navigator_bluetooth.cc",
    "render/modules/credentialmanager/credentials_container.cc",
    "render/modules/battery/navigator_battery.cc",
  ]

  deps = [
    "//third_party/blink/public:blink",
  ]
}
